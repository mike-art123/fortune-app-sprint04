# design_tokens
Single source of truth for the **Illuminated Sky** visual language.
`tokens.json` is authoritative; platform files (Dart/CSS/Figma) are generated from it.
Rule: never hardcode colors/spacing in app code — consume tokens only.
